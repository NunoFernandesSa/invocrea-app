import { InvoiceInfoProps } from "@/src/types/invoice-info-type-props";
import React from "react";

/**
 * InvoiceInfo component
 * @description Component to display invoice information
 * @param {InvoiceInfoProps} props - The props of the component
 * @returns {JSX.Element} The JSX element of the component
 */
export default function InvoiceInfo({ invoice, setInvoice }: InvoiceInfoProps) {
  return <div>InvoiceInfo</div>;
}
